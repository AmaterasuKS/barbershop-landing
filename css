@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800&display=swap');
:root{
  --bg:#0b0b0b;
  --card:#121212;
  --muted:#9aa0a6;
  --accent:#e63946;
  --accent-2:#ff4d5a;
  --glass: rgba(255,255,255,0.04);
  --white: #fff;
  --maxw:1100px;
}
*{box-sizing:border-box}
html,body{height:100%;}
body{
  margin:0;
  font-family:Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
  background:linear-gradient(180deg,#060606 0%, #0b0b0b 100%);
  color:var(--white);
  -webkit-font-smoothing:antialiased;
  -moz-osx-font-smoothing:grayscale;
  line-height:1.5;
}
a{color:inherit;text-decoration:none}
.container{max-width:var(--maxw);margin:0 auto;padding:32px;}
header{position:sticky;top:0;z-index:40;background:linear-gradient(180deg, rgba(11,11,11,0.9), rgba(11,11,11,0.6));backdrop-filter: blur(6px);border-bottom:1px solid rgba(255,255,255,0.03)}
.header-inner{display:flex;align-items:center;justify-content:space-between;max-width:var(--maxw);margin:0 auto;padding:14px 24px;}
.brand{display:flex;gap:12px;align-items:center}
.logo{width:48px;height:48px;border-radius:8px;background:linear-gradient(135deg,var(--accent),var(--accent-2));display:flex;align-items:center;justify-content:center;font-weight:800;font-size:20px;color:#fff;box-shadow:0 6px 20px rgba(230,57,70,0.12);}
.brand .title{font-weight:700}
nav{display:flex;gap:18px;align-items:center}
nav a{font-weight:600;color:rgba(255,255,255,0.88);font-size:14px}
.cta{background:var(--accent);padding:10px 14px;border-radius:8px;color:#fff;font-weight:700;box-shadow:0 6px 18px rgba(230,57,70,0.12)}
.cta:hover{background:var(--accent-2)}
.hero{display:flex;align-items:center;justify-content:center;min-height:72vh;padding:60px 20px;background-image: radial-gradient( circle at top left, rgba(230,57,70,0.06), transparent 10% ), radial-gradient( circle at bottom right, rgba(255,77,90,0.04), transparent 20% );}
.hero-inner{display:flex;gap:40px;align-items:center;justify-content:space-between;max-width:var(--maxw);width:100%}
.hero-left{flex:1;min-width:260px}
.eyebrow{display:inline-block;padding:6px 10px;background:var(--glass);border-radius:999px;color:var(--muted);font-size:13px;margin-bottom:16px}
h1{font-size:40px;margin:0 0 12px 0;line-height:1.02}
p.lead{color:var(--muted);max-width:620px}
.hero-actions{margin-top:20px;display:flex;gap:12px;align-items:center}
.btn{background:var(--accent);color:white;padding:12px 18px;border-radius:10px;border:none;font-weight:700;cursor:pointer}
.btn.ghost{background:transparent;border:1px solid rgba(255,255,255,0.06);padding:10px 14px}
.hero-right{width:420px;max-width:44%;min-width:260px}
.card{background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));border-radius:14px;padding:18px;border:1px solid rgba(255,255,255,0.03);box-shadow: 0 8px 30px rgba(2,2,2,0.6);}
.price{font-size:20px;font-weight:800;color:var(--accent)}
.list{display:flex;flex-direction:column;gap:10px;margin-top:12px}
.list .item{display:flex;justify-content:space-between;align-items:center;padding:8px 10px;border-radius:8px;background:rgba(255,255,255,0.01)}
.small{font-size:13px;color:var(--muted)}
.section{padding:60px 0}
.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:18px}
.service{background:var(--card);padding:22px;border-radius:12px;border:1px solid rgba(255,255,255,0.02)}
.service h4{margin:0 0 8px 0;color:var(--accent)}
.service p{color:var(--muted);margin:0}
.samples{display:grid;grid-template-columns:repeat(3,1fr);gap:18px}
.sample{background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.015));border-radius:12px;padding:14px;border:1px solid rgba(255,255,255,0.03)}
.sample .thumb{height:120px;border-radius:8px;background:linear-gradient(135deg,#222,#111);display:flex;align-items:center;justify-content:center;color:var(--muted);font-weight:700}
.about{display:flex;gap:26px;align-items:center}
.about .text{flex:1;color:var(--muted)}
.contact-wrap{display:grid;grid-template-columns:1fr 360px;gap:20px}
form{display:flex;flex-direction:column;gap:12px}
input,textarea,select{background:transparent;border:1px solid rgba(255,255,255,0.06);padding:12px;border-radius:8px;color:var(--white);outline:none}
textarea{min-height:120px;resize:vertical}
.muted{color:var(--muted);font-size:13px}
.foot{padding:24px;text-align:center;color:var(--muted);font-size:14px}
@media (max-width:980px){.hero-inner{flex-direction:column-reverse;align-items:center}.hero-right{max-width:100%}.grid{grid-template-columns:repeat(2,1fr)}.samples{grid-template-columns:repeat(2,1fr)}.contact-wrap{grid-template-columns:1fr}}
@media (max-width:560px){h1{font-size:28px}.grid{grid-template-columns:1fr}.samples{grid-template-columns:1fr}nav{display:none}.header-inner{padding:12px}.hero{padding:30px 12px}.logo{width:42px;height:42px;font-size:18px}}
.fade-in{opacity:0;transform:translateY(10px);animation:fadeIn .7s forwards}
.stagger-1{animation-delay:.08s}
.stagger-2{animation-delay:.16s}
.stagger-3{animation-delay:.24s}
@keyframes fadeIn{to{opacity:1;transform:none}}
.inline-link{color:var(--accent);font-weight:700}
